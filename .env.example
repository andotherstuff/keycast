# ABOUTME: Environment variable examples for Keycast configuration
# ABOUTME: Copy to .env and set your actual values for Google Cloud KMS

# Database configuration
DATABASE_URL=sqlite:../database/keycast.db

# Key management configuration
USE_GCP_KMS=false

# Google Cloud KMS configuration (only needed if USE_GCP_KMS=true)
GCP_PROJECT_ID=openvine-co
GCP_KMS_LOCATION=global
GCP_KMS_KEY_RING=keycast-keys
GCP_KMS_KEY_NAME=master-key

# Google Cloud authentication (handled by Application Default Credentials)
# Run: gcloud auth application-default login
# Or set GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Signer daemon configuration
AUTH_ID=1